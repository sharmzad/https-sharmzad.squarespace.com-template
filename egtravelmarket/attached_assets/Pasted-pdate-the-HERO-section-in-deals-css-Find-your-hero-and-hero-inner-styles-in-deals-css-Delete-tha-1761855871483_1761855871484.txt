pdate the HERO section in deals.css

Find your .hero and .hero-inner styles in deals.css.
Delete that whole .hero block and replace it with this new one:

/* ===== Cinematic Hero Header ===== */
.hero {
  position: relative;
  padding: 90px 20px 70px;
  text-align: center;
  color: #eaf0ff;
  background: radial-gradient(circle at 20% 30%, rgba(0,212,255,0.15), transparent 40%),
              radial-gradient(circle at 80% 20%, rgba(44,125,240,0.25), transparent 40%),
              linear-gradient(180deg, #08111f 0%, #0b1220 100%);
  overflow: hidden;
  border-bottom: 1px solid rgba(255,255,255,0.05);
}

/* Floating light beams animation */
.hero::before,
.hero::after {
  content: "";
  position: absolute;
  top: -50%;
  left: -50%;
  width: 200%;
  height: 200%;
  background: repeating-linear-gradient(
    115deg,
    rgba(0,212,255,0.03) 0px,
    rgba(0,212,255,0.03) 2px,
    transparent 4px,
    transparent 8px
  );
  transform: rotate(15deg);
  animation: moveBeams 30s linear infinite;
}

.hero::after {
  opacity: 0.6;
  animation-direction: reverse;
  animation-duration: 45s;
}

@keyframes moveBeams {
  0% { transform: translateY(0) rotate(15deg); }
  100% { transform: translateY(50%) rotate(15deg); }
}

.hero-inner {
  position: relative;
  z-index: 2;
  max-width: 1100px;
  margin: 0 auto;
}

.hero h1 {
  font-size: clamp(28px, 4vw, 48px);
  font-weight: 700;
  background: linear-gradient(135deg, #2c7df0, #00d4ff);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  margin-bottom: 12px;
}

.hero p {
  color: rgba(234,240,255,0.8);
  max-width: 650px;
  margin: 0 auto 24px;
  font-size: 17px;
  line-height: 1.5;
}

.hero .hero-cta {
  display: flex;
  gap: 12px;
  justify-content: center;
  flex-wrap: wrap;
}

🎇 Step 2 — Add glow on buttons (optional but stylish)

Still in deals.css, add this below your .btn class:

.btn {
  background: linear-gradient(135deg,#2c7df0,#00d4ff);
  color:#031021;border:0;padding:12px 18px;border-radius:999px;
  font-weight:600;box-shadow:0 0 12px rgba(0,212,255,0.4), 0 10px 30px rgba(0,0,0,0.25);
  display:inline-block;transition:transform 0.2s ease, box-shadow 0.2s ease;
}
.btn:hover {
  transform: translateY(-3px);
  box-shadow:0 0 20px rgba(0,212,255,0.6), 0 10px 35px rgba(0,0,0,0.35);
}